# Terraform and provider version pinning.
# Pinning versions makes builds reproducible in teams and CI pipelines.
terraform {
  required_version = ">= 1.6.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

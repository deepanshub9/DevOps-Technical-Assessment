# PodDisruptionBudget keeps at least one Tomcat pod running during voluntary disruptions.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tomcat-pdb
  namespace: app-stack
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: tomcat
